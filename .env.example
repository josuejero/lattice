# --- Core services (required for dev auth mode) ---
NODE_ENV="development"
DATABASE_URL="postgresql://lattice:lattice@localhost:5432/lattice"
REDIS_URL="redis://localhost:6379"
AUTH_SECRET="replace-with-a-long-random-string"
# Public URL is used for links/callbacks; match whatever port you run the dev server on.
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# --- Auth.js / NextAuth ---
# Dev credentials (email-only) work without Google OAuth, so the Google keys stay blank for local-only mode.
# Fill these once you decide to enable Google OAuth too.
AUTH_GOOGLE_ID=""
AUTH_GOOGLE_SECRET=""

# --- Encryption helpers (required once calendar encryption is enabled) ---
# TOKEN_ENC_KEY must decode to 32 raw bytes (some docs call this the ENCRYPTION_KEY); leave blank until you need GCAL encryption.
TOKEN_ENC_KEY=""

# --- Optional third-party services ---
# RESEND_API_KEY=""
# SENTRY_DSN=""

# --- Google Calendar integration (Phase 4) ---
GCAL_CLIENT_ID=""
GCAL_CLIENT_SECRET=""
GCAL_REDIRECT_URI="http://localhost:3000/api/integrations/google/callback"

# --- Feature flags ---
SUGGESTIONS_ENABLED="1"
EVENTS_ENABLED="1"
GCAL_WRITEBACK_ENABLED="0"
